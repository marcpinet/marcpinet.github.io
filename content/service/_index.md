+++
title = "Service"
template = "service.html"
sort_by = "date"

# This section IS the /service/ page (rendered by service.html), and ALSO holds the
# "Reviewing" entries. Add a reviewing/PC/organizing role as content/service/<slug>.md with:
#   title (the role, e.g. "Ethics Reviewer"), date (ISO), [extra]: venue? (e.g. "NeurIPS 2026"),
#   link?, image?. Shows under Reviewing on the Service page + as a News line.
#   The News thumbnail FILLS (crops to fit) by default; add `logo = true` to show a logo whole
#   (contained, padded) instead of cropping it.
# Talks / Teaching / Supervision also live on this page (their own content/<section>/ folders).
+++
